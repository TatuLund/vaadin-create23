# VaadinCreate backend data model

This diagram is generated by inspecting the entity classes in `vaadincreate-backend/src/main/java/org/vaadin/tatu/vaadincreate/backend/data`.

- Mermaid source: [DataModel.mmd](DataModel.mmd)
- Rendered diagram: [DataModel.svg](DataModel.svg)

## Notes / interpretation

- Persistent JPA entities: `User`, `UserSupervisor`, `Purchase`, `PurchaseLine`, `Product`, `Category`, `Draft`, `Message`.
- Value object: `Address` is `@Embeddable` and is stored as columns on `Purchase` (street/postal_code/city/country).
- Enums stored as strings: `Availability`, `PurchaseStatus`, `User.Role`.
- `Cart` is an in-memory model only (not a database entity).
- `Draft.productId` is stored as an integer column and is not mapped as a JPA relationship to `Product`.
- Unique constraints:
  - `uq_application_user_user_name` on `application_user(user_name)`.
  - `uq_category_category_name` on `category(category_name)`.
- Indexes (query/performance):
  - `idx_purchase_requester_created_at` on `purchase(requester_id, created_at)`.
  - `idx_purchase_approver_status_created_at` on `purchase(approver_id, status, created_at)`.
  - `idx_purchase_created_at` on `purchase(created_at)`.
  - `idx_purchase_status_decided_at` on `purchase(status, decided_at)`.
  - `idx_purchase_line_purchase_id` on `purchase_line(purchase_id)`.

## ER diagram (Mermaid)

```mermaid
erDiagram
  APPLICATION_USER {
    int id PK
    int version
    string user_name "unique"
    string passwd
    string role
    datetime last_status_check
    boolean active
  }

  USER_SUPERVISOR {
    int id PK
    int version
    int employee_id FK
    int supervisor_id FK
  }

  PURCHASE {
    int id PK
    int version
    int requester_id FK
    int approver_id FK "nullable"
    string status
    datetime created_at
    datetime decided_at "nullable"
    string decision_reason "nullable"
    string street
    string postal_code
    string city
    string country
  }

  PURCHASE_LINE {
    int id PK
    int version
    int purchase_id FK
    int product_id FK
    int quantity
    decimal unit_price
  }

  PRODUCT {
    int id PK
    int version
    string product_name
    decimal price
    int stock_count
    string availability
  }

  CATEGORY {
    int id PK
    int version
    string category_name "unique"
  }

  DRAFT {
    int id PK
    int version
    int product_id "not mapped as FK"
    int user_id FK
    string product_name
    decimal price
    int stock_count
    string availability
  }

  MESSAGE {
    int id PK
    int version
    string message
    datetime date_stamp
  }

  APPLICATION_USER ||--o{ PURCHASE : requester
  APPLICATION_USER o|--o{ PURCHASE : approver

  PURCHASE ||--|{ PURCHASE_LINE : lines
  PRODUCT  ||--o{ PURCHASE_LINE : product

  PRODUCT }o--o{ CATEGORY : product_category
  DRAFT   }o--o{ CATEGORY : draft_category

  APPLICATION_USER o|--|| DRAFT : draft_user

  APPLICATION_USER ||--o{ USER_SUPERVISOR : employee
  APPLICATION_USER ||--o{ USER_SUPERVISOR : supervisor
```
